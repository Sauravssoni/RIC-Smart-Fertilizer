const DISTRICTS = [{"id": "jaipur", "name": "Jaipur", "zone": "Central", "x": 378, "y": 232, "fert": "DAP", "stock": 0.081, "p50": 0.069, "p90": 0.094, "cover": 5.1, "risk": "critical", "lead": 1.8, "trace": 2, "reason": "Mustard sowing acceleration + rainfall recovery"}, {"id": "sikar", "name": "Sikar", "zone": "North-East", "x": 345, "y": 171, "fert": "DAP", "stock": 0.052, "p50": 0.048, "p90": 0.067, "cover": 4.4, "risk": "critical", "lead": 2.1, "trace": 1, "reason": "DAP velocity + SHC phosphorus deficiency cluster"}, {"id": "jodhpur", "name": "Jodhpur", "zone": "West", "x": 222, "y": 268, "fert": "UREA", "stock": 0.119, "p50": 0.091, "p90": 0.121, "cover": 8.7, "risk": "watch", "lead": 2.9, "trace": 0, "reason": "Bajra area + sales velocity above seasonal baseline"}, {"id": "barmer", "name": "Barmer", "zone": "Far West", "x": 152, "y": 326, "fert": "UREA", "stock": 0.082, "p50": 0.067, "p90": 0.088, "cover": 9.6, "risk": "watch", "lead": 3.6, "trace": 1, "reason": "Longer logistics lead time; protect service floor"}, {"id": "kota", "name": "Kota", "zone": "South-East", "x": 465, "y": 340, "fert": "NPKS", "stock": 0.094, "p50": 0.061, "p90": 0.073, "cover": 14.9, "risk": "stable", "lead": 1.4, "trace": 0, "reason": "Stable stock, candidate source node"}, {"id": "udaipur", "name": "Udaipur", "zone": "South", "x": 317, "y": 389, "fert": "NPKS", "stock": 0.071, "p50": 0.059, "p90": 0.071, "cover": 10.2, "risk": "watch", "lead": 2.2, "trace": 0, "reason": "Crop-stage transition approaching"}, {"id": "bikaner", "name": "Bikaner", "zone": "North-West", "x": 244, "y": 148, "fert": "UREA", "stock": 0.132, "p50": 0.082, "p90": 0.099, "cover": 16.2, "risk": "stable", "lead": 2.7, "trace": 0, "reason": "Adequate cover, monitor transport window"}, {"id": "ganganagar", "name": "Sri Ganganagar", "zone": "North", "x": 242, "y": 82, "fert": "UREA", "stock": 0.102, "p50": 0.072, "p90": 0.089, "cover": 13.1, "risk": "stable", "lead": 2.4, "trace": 0, "reason": "Stable forecast and warehouse coverage"}, {"id": "bharatpur", "name": "Bharatpur", "zone": "East", "x": 492, "y": 207, "fert": "DAP", "stock": 0.061, "p50": 0.052, "p90": 0.072, "cover": 6.8, "risk": "high", "lead": 1.7, "trace": 2, "reason": "DAP demand acceleration + two trace exceptions"}, {"id": "ajmer", "name": "Ajmer", "zone": "Central", "x": 336, "y": 281, "fert": "DAP", "stock": 0.076, "p50": 0.057, "p90": 0.071, "cover": 9.4, "risk": "watch", "lead": 1.6, "trace": 0, "reason": "Balanced position; reserve source option"}, {"id": "alwar", "name": "Alwar", "zone": "North-East", "x": 446, "y": 166, "fert": "DAP", "stock": 0.064, "p50": 0.052, "p90": 0.068, "cover": 7.3, "risk": "high", "lead": 1.5, "trace": 1, "reason": "Phosphorus demand + route exception"}, {"id": "chittor", "name": "Chittorgarh", "zone": "South-East", "x": 398, "y": 365, "fert": "NPKS", "stock": 0.083, "p50": 0.054, "p90": 0.066, "cover": 15.1, "risk": "stable", "lead": 1.7, "trace": 0, "reason": "Surplus under demo service-floor constraint"}];
const WAREHOUSES = [{"id": "WH-JPR-01", "name": "Jaipur Central Warehouse", "district": "Jaipur", "fert": "DAP", "physical": 4820, "digital": 4791, "cover": 5.1, "status": "Attention", "gps": "Online"}, {"id": "WH-KOT-02", "name": "Kota Regional Warehouse", "district": "Kota", "fert": "NPKS", "physical": 7340, "digital": 7338, "cover": 14.9, "status": "Healthy", "gps": "Online"}, {"id": "WH-BKN-01", "name": "Bikaner Regional Warehouse", "district": "Bikaner", "fert": "UREA", "physical": 9480, "digital": 9479, "cover": 16.2, "status": "Healthy", "gps": "Online"}, {"id": "WH-BTP-01", "name": "Bharatpur Warehouse", "district": "Bharatpur", "fert": "DAP", "physical": 3230, "digital": 3154, "cover": 6.8, "status": "Mismatch", "gps": "Online"}, {"id": "WH-UDP-01", "name": "Udaipur Warehouse", "district": "Udaipur", "fert": "NPKS", "physical": 4160, "digital": 4155, "cover": 10.2, "status": "Watch", "gps": "Degraded"}];
const FARMERS = [{"fid": "FR-RJ-2048-0194", "name": "Demo Farmer 0194", "district": "Jaipur", "crop": "Mustard", "stage": "28 DAS", "shc": "SHC-RJ-88421", "freshness": "92 days", "risk": "P low · OC low", "status": "Verified"}, {"fid": "FR-RJ-2048-0211", "name": "Demo Farmer 0211", "district": "Sikar", "crop": "Mustard", "stage": "35 DAS", "shc": "SHC-RJ-99214", "freshness": "211 days", "risk": "P low", "status": "SHC review"}, {"fid": "FR-RJ-2048-0310", "name": "Demo Farmer 0310", "district": "Jodhpur", "crop": "Bajra", "stage": "42 DAS", "shc": "SHC-RJ-77301", "freshness": "141 days", "risk": "N moderate", "status": "Verified"}];
const PUBLIC={urea:{req:6.71,avail:11.31,sales:8.05,stock:3.27},dap:{req:3.21,avail:2.73,sales:2.08,stock:.65},mop:{req:.05,avail:.15,sales:.05,stock:.10},npks:{req:.77,avail:1.40,sales:.68,stock:.72}};
const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
let state={selected:'sikar',mapMode:'risk',transfers:[],demo:false,demoTimer:null,sutraStep:0,synced:false,tampered:false};
const pageTitles={command:'State Fertilizer Command Centre',demand:'Demand & Allocation Intelligence',inventory:'Inventory & Movement Control',farmer:'Farmer / Plot Intelligence',trace:'Trace, Quality & Leakage Assurance',sutra:'SUTRA-ID Edge Field Operations',governance:'Integrations & Governance'};
function toast(msg){let t=$('#toast');t.textContent=msg;t.classList.add('show');clearTimeout(toast._t);toast._t=setTimeout(()=>t.classList.remove('show'),2300)}
function nav(page){$$('.page,.nav button').forEach(x=>x.classList.remove('active'));$('#'+page).classList.add('active');$(`.nav button[data-page="${page}"]`).classList.add('active');$('#pageTitle').textContent=pageTitles[page];window.scrollTo(0,0)}
$$('.nav button').forEach(b=>b.onclick=()=>nav(b.dataset.page));
function fmt(x){return x.toFixed(3)+' LMT'}
function riskClass(d){return d.risk}
function coverClass(c){return c<6?'low':c<10?'med':''}
function riskScore(d){return d.risk==='critical'?96:d.risk==='high'?78:d.risk==='watch'?56:24}
function markerColorClass(d){if(state.mapMode==='risk')return d.risk;if(state.mapMode==='stock')return d.cover<6?'critical':d.cover<10?'watch':'stable';return d.trace>=2?'critical':d.trace===1?'watch':'stable'}
function markerSub(d){if(state.mapMode==='risk')return 'P90 '+d.cover.toFixed(1)+'d';if(state.mapMode==='stock')return d.cover.toFixed(1)+'d cover';return d.trace+' trace alert'+(d.trace===1?'':'s')}
function renderMap(){let g=$('#markers');g.innerHTML=DISTRICTS.map(d=>`<g class="marker ${markerColorClass(d)} ${state.selected===d.id?'selected':''}" data-id="${d.id}"><circle class="pulse" cx="${d.x}" cy="${d.y}" r="14"></circle><circle class="core" cx="${d.x}" cy="${d.y}" r="7"></circle><text x="${d.x+11}" y="${d.y-2}">${d.name}</text><text class="sub" x="${d.x+11}" y="${d.y+9}">${markerSub(d)}</text></g>`).join('');$$('.marker',g).forEach(m=>m.onclick=()=>selectDistrict(m.dataset.id))}
function renderRows(){let tb=$('#districtRows');tb.innerHTML=DISTRICTS.map(d=>`<tr data-id="${d.id}" class="${state.selected===d.id?'selected':''}"><td><strong>${d.name}</strong><br><span style="color:#8193a3">${d.zone}</span></td><td>${d.fert}</td><td>${fmt(d.stock)}</td><td>${fmt(d.p50)} / <b>${fmt(d.p90)}</b></td><td>${d.cover.toFixed(1)} d<div class="coverbar ${coverClass(d.cover)}"><i style="width:${Math.min(100,d.cover/18*100)}%"></i></div></td><td>${d.lead.toFixed(1)} d</td><td>${d.trace}</td><td><span class="badge ${d.risk}">${d.risk}</span></td><td><button class="actionlink">Inspect →</button></td></tr>`).join('');$$('tbody tr',tb).forEach(r=>r.onclick=()=>selectDistrict(r.dataset.id))}
function selected(){return DISTRICTS.find(d=>d.id===state.selected)||DISTRICTS[0]}
function selectDistrict(id){state.selected=id;renderMap();renderRows();renderWorkbench();}
function transferSuggestion(d){let source=DISTRICTS.filter(x=>x.fert===d.fert&&x.id!==d.id).sort((a,b)=>b.cover-a.cover)[0];let qty=Math.max(.012,Math.min(.045,(d.p90-d.stock)*.65));return {source,qty};}
function renderWorkbench(){let d=selected(), t=transferSuggestion(d);let score=riskScore(d);let sourceAfter=Math.max(0,t.source.cover-(t.qty/(t.source.stock||.1))*4.2);$('#workbench').innerHTML=`<div class="wb-title"><div><h3>${d.name} · ${d.fert}</h3><p>${d.zone} · DEMO_SANDBOX operational cluster</p></div><span class="badge ${d.risk}">${d.risk}</span></div><div class="meta-grid"><div class="meta"><span>P90 cover</span><b>${d.cover.toFixed(1)} days</b></div><div class="meta"><span>4-week P90</span><b>${fmt(d.p90)}</b></div><div class="meta"><span>Local stock</span><b>${fmt(d.stock)}</b></div><div class="meta"><span>Risk score</span><b>${score}/100</b></div></div><div class="signal-list"><div class="signal"><b>Top causal signal</b><span>${d.reason}</span></div><div class="signal"><b>Trace / inventory context</b><span>${d.trace} open custody exception(s); lead time ${d.lead.toFixed(1)} days.</span></div><div class="signal"><b>Fairness guardrail</b><span>Proposed source ${t.source.name} remains ${sourceAfter.toFixed(1)} days above minimum source cover in this demo.</span></div></div><div class="rec"><b>Recommended officer action</b>${d.cover<8?`Stage ${fmt(t.qty)} ${d.fert} transfer from ${t.source.name}. Require source-floor validation + reason-coded approval.`:'Monitor only; no stock movement recommended at current P90 service level.'}</div>${d.cover<8?`<div class="btnrow"><button class="btn primary" id="approveBtn">Approve transfer</button><button class="btn outline" id="overrideBtn">Override / defer</button></div>`:`<button class="btn outline full" id="monitorBtn">Record monitoring decision</button>`}<div class="decision-receipt" id="receipt"><b>DECISION RECEIPT CREATED</b><code id="receiptCode"></code></div>`;
 if($('#approveBtn'))$('#approveBtn').onclick=()=>approveTransfer(d,t);
 if($('#overrideBtn'))$('#overrideBtn').onclick=()=>recordOverride(d);
 if($('#monitorBtn'))$('#monitorBtn').onclick=()=>{toast('Monitoring decision recorded');showReceipt(d,'MONITOR','No transfer issued')};
}
function approveTransfer(d,t){let id='TR-RJ-'+String(Date.now()).slice(-7);state.transfers.unshift({id,from:t.source.name,to:d.name,fert:d.fert,qty:t.qty,status:'AUTHORIZED',time:new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit'})});d.stock+=t.qty;d.cover+=2.4;t.source.stock-=t.qty;t.source.cover=Math.max(7.2,t.source.cover-1.8);d.risk=d.cover<6?'critical':d.cover<8?'high':d.cover<10?'watch':'stable';let receipt=`${id} | ${d.name} | ${d.fert} | ${fmt(t.qty)} | AUTHORIZED | guardrail=PASS | hash=${simpleHash(id+d.name+t.qty)}`;$('#receipt').classList.add('show');$('#receiptCode').textContent=receipt;addFeed('good','TRANSFER AUTHORIZED',`${fmt(t.qty)} ${d.fert}: ${t.source.name} → ${d.name}`,'DECISION');renderMap();renderRows();renderTransfers();updateKPIs();toast('Human-approved transfer staged');}
function recordOverride(d){showReceipt(d,'OVERRIDE','Officer deferred recommendation; reason code DEMO-OPS-04');addFeed('warn','RECOMMENDATION OVERRIDDEN',`${d.name} transfer deferred with reason code`,'OFFICER')}
function showReceipt(d,decision,note){let code=`DR-${String(Date.now()).slice(-8)} | ${d.name} | ${decision} | ${note} | hash=${simpleHash(d.name+decision+note)}`;$('#receipt').classList.add('show');$('#receiptCode').textContent=code;}
function simpleHash(s){let h=2166136261;for(let i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619)}return (h>>>0).toString(16).padStart(8,'0')}
const feedBase=[
 ['bad','DAP COVER BREACH WATCH','Sikar P90 stock cover below 5 days','DEMAND'],
 ['warn','TRACE EXCEPTION','Bharatpur lot receipt variance requires review','TRACE'],
 ['good','WAREHOUSE RECEIPT','Kota NPKS digital / physical match confirmed','INVENTORY'],
 ['info','SUTRA SYNC','Offline field receipt queued at Jaipur dealer point','EDGE'],
 ['warn','MODEL DRIFT WATCH','DAP sales velocity feature shifted +1.4σ','MODEL'],
 ['good','QUALITY SAMPLE','Lot DAP-26-RJ-1842 lab evidence hash linked','QUALITY']
];
function renderFeed(){let now=Date.now();$('#feed').innerHTML=feedBase.map((f,i)=>feedHTML(f[0],new Date(now-i*7*60000),f[1],f[2],f[3])).join('')}
function feedHTML(level,time,title,copy,type){let cls=level==='bad'?'red':level==='warn'?'amber':'green';return `<div class="feedrow"><i class="ficon ${cls}"></i><time>${time.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</time><div><b>${title}</b><p>${copy}</p></div><span class="badge ${level==='bad'?'critical':level==='warn'?'watch':'info'}">${type}</span></div>`}
function addFeed(level,title,copy,type){let row=document.createElement('div');row.innerHTML=feedHTML(level,new Date(),title,copy,type);$('#feed').prepend(row.firstElementChild)}
function updateKPIs(){let crit=DISTRICTS.filter(d=>d.risk==='critical'||d.risk==='high').length;$('#kpiCritical').textContent=crit;$('#kpiTrace').textContent=DISTRICTS.reduce((s,d)=>s+d.trace,0);$('#kpiTransit').innerHTML=(.18+state.transfers.reduce((s,t)=>s+t.qty,0)).toFixed(2)+' <small>LMT</small>';}
function renderWarehouses(){ $('#warehouseRows').innerHTML=WAREHOUSES.map(w=>{let v=w.physical-w.digital;return `<tr><td><strong>${w.name}</strong><br><span style="color:#8091a0">${w.id}</span></td><td>${w.district}</td><td>${w.fert}</td><td>${w.physical.toLocaleString()}</td><td>${w.digital.toLocaleString()}</td><td style="color:${Math.abs(v)>50?'#b73f49':'#11764b'}">${v>0?'+':''}${v}</td><td>${w.cover.toFixed(1)} d</td><td>${w.gps}</td><td><span class="badge ${w.status==='Healthy'?'healthy':w.status==='Mismatch'?'critical':'watch'}">${w.status}</span></td></tr>`}).join('') }
function renderTransfers(){let el=$('#transferOrders');if(!state.transfers.length){el.innerHTML='<div class="subtext">No active demo transfer orders. Approve a recommendation from the Command Centre to create one.</div>'}else el.innerHTML=state.transfers.map(t=>`<div class="signal"><b>${t.id} · ${t.fert} · ${fmt(t.qty)}</b><span>${t.from} → ${t.to} · ${t.status} · ${t.time}</span></div>`).join('')}
function renderLogistics(){let q=[['Bharatpur','Digital / physical stock variance 76 bags','HIGH'],['Udaipur','GPS telemetry degraded; mobile scan fallback active','WATCH'],['Jaipur','Inbound DAP transfer ETA 1.8 days','INFO']];$('#logisticsQueue').innerHTML=q.map(x=>`<div class="signal"><b>${x[0]} <span class="badge ${x[2]==='HIGH'?'critical':x[2]==='WATCH'?'watch':'info'}">${x[2]}</span></b><span>${x[1]}</span></div>`).join('')}
function populateForecast(){let s=$('#forecastDistrict');s.innerHTML=DISTRICTS.map(d=>`<option value="${d.id}">${d.name}</option>`).join('');s.value='sikar';['areaRange','rainRange','salesRange','forecastDistrict','forecastFert'].forEach(id=>$('#'+id).oninput=renderForecast);$('#forecastBtn').onclick=()=>{renderForecast();toast('Forecast envelope recomputed')};renderForecast()}
function renderForecast(){let d=DISTRICTS.find(x=>x.id===$('#forecastDistrict').value)||DISTRICTS[0],fert=$('#forecastFert').value;let area=+$('#areaRange').value,rain=+$('#rainRange').value,sales=+$('#salesRange').value;$('#areaOut').textContent=(area>=0?'+':'')+area+'%';$('#rainOut').textContent=(rain>=0?'+':'')+rain+'%';$('#salesOut').textContent=(sales>=0?'+':'')+sales+'%';let base={DAP:.062,UREA:.115,NPKS:.044,MOP:.011}[fert]*(1+(d.p90/.1)*.16);let p50=base*(1+area/100)*(1+Math.max(-.16,Math.min(.18,rain*.003)))*(1+sales*.004);let spread=p50*(.13+Math.abs(rain)*.0015);let vals=[Math.max(0,p50-spread*1.2),p50,p50+spread*1.35];$('#quantiles').innerHTML=['P10','P50','P90'].map((q,i)=>`<div class="q"><span>${q}</span><b>${vals[i].toFixed(3)}</b><small style="font-size:6px;color:#7b8d9d">LMT · 4 weeks</small></div>`).join('');let xs=[45,125,205,285,365,445,525],mid=[.54,.64,.62,.73,.81,.88,.92].map(v=>v*(1+area/180+sales/300)),hi=mid.map(v=>Math.min(.99,v+.11+Math.abs(rain)/400)),lo=mid.map(v=>Math.max(.15,v-.1-Math.abs(rain)/450));let y=v=>160-v*125;let path=a=>a.map((v,i)=>(i?'L':'M')+xs[i]+' '+y(v)).join(' ');let band=path(hi)+' '+lo.map((v,i)=>`L ${xs[lo.length-1-i]} ${y(lo[lo.length-1-i])}`).join(' ')+' Z';$('#forecastChart').innerHTML=`${[35,70,105,140].map(v=>`<line class="gridline" x1="35" y1="${v}" x2="560" y2="${v}"></line>`).join('')}<path class="forecastband" d="${band}"></path><path class="forecastline" d="${path(mid)}"></path><path class="actual" d="${path(mid.map((v,i)=>v*(i<4?.93:1.01)))}"></path>`;$('#forecastExplain').innerHTML=`<b>${d.name} · ${fert}</b>: P90 moves to <b>${vals[2].toFixed(3)} LMT</b>. Top demo drivers: crop area ${area>=0?'+':''}${area}%, rainfall ${rain>=0?'+':''}${rain}%, sales velocity ${sales>=0?'+':''}${sales}%. Production release requires rolling-origin and district-holdout gates.`}
function lookupFarmer(){let q=$('#farmerSearch').value.trim().toLowerCase();let f=FARMERS.find(x=>x.fid.toLowerCase()===q||x.name.toLowerCase().includes(q))||FARMERS[0];$('#farmerResult').innerHTML=`<div class="meta-grid"><div class="meta"><span>Farmer reference</span><b>${f.fid}</b></div><div class="meta"><span>Status</span><b>${f.status}</b></div><div class="meta"><span>District / crop</span><b>${f.district} · ${f.crop}</b></div><div class="meta"><span>Crop stage</span><b>${f.stage}</b></div></div>`;$('#passport').innerHTML=`<div class="meta-grid"><div class="meta"><span>SHC reference</span><b>${f.shc}</b></div><div class="meta"><span>SHC freshness</span><b>${f.freshness}</b></div><div class="meta"><span>Deficiency context</span><b>${f.risk}</b></div><div class="meta"><span>Crop / stage</span><b>${f.crop} · ${f.stage}</b></div></div><div class="rec"><b>Guarded production path</b>Authorized SHC / State agronomy service → crop-stage rule → permitted-product optimizer → RAJ-NUTRI grounded explanation. No numeric dose is invented by the SLM.</div>`}
$('#lookupFarmer').onclick=lookupFarmer;
const traceEvents=[['08:42','DISPATCH','Manufacturer / railhead lot sealed','9f3a21'],['13:18','WAREHOUSE RECEIPT','QR + weighbridge evidence','2c91d0'],['09:14','TRANSFER','Vehicle / geofence ref pseudonymized','49aa72'],['11:02','DEALER RECEIPT','Lot scan accepted','0bf882'],['15:37','RETAIL LINK','FFS / PoS transaction reference linked','17cc44'],['16:04','FIELD ACK','SUTRA signed receipt synchronized','8ae77b']];
function renderTimeline(){let bad=state.tampered;$('#timeline').innerHTML=traceEvents.map((e,i)=>`<div class="trace-event" style="${bad&&i===2?'border-color:#e3a8ad;background:#fff3f4':''}"><time>${e[0]}</time><b>${e[1]} ${bad&&i===2?'⚠':''}</b><p>${bad&&i===2?'Hash-chain mismatch detected after simulated mutation':e[2]}</p><code>${bad&&i===2?'INVALID:'+e[3]:e[3]}</code></div>`).join('');$('#qualityBadge').className='badge '+(bad?'critical':'healthy');$('#qualityBadge').textContent=bad?'Review priority: HIGH':'Review priority: low';}
$('#tamperBtn').onclick=()=>{state.tampered=!state.tampered;renderTimeline();addFeed(state.tampered?'bad':'good',state.tampered?'TRACE TAMPER DETECTED':'TRACE CHAIN RESTORED','TraceGraph hash verification updated','TRACE');toast(state.tampered?'Tamper detected and routed to human review':'Trace chain restored')};
$('#scoreBtn').onclick=()=>{let r=(+$('#volSel').value>2?30:0)+(+$('#routeSel').value)+(+$('#stockSel').value)+(+$('#patternSel').value);r=Math.min(100,r);$('#sentinelOut').innerHTML=`<b>${r>=60?'HIGH-PRIORITY HUMAN REVIEW':r>=35?'HUMAN REVIEW':'MONITOR'} · ${r}/100</b><br>Explainable anomaly score only. No automatic subsidy denial, dealer accusation or enforcement action.`;toast('Sentinel human-review priority generated')};
function renderSync(){let rows=[['EVT-SUTRA-74291','SIGNED_LOCALLY',state.synced?'ACKNOWLEDGED':'QUEUED'],['EVT-SUTRA-74291 retry','IDEMPOTENCY_KEY',state.synced?'DUPLICATE_SUPPRESSED':'PENDING']];$('#syncLedger').innerHTML=rows.map(x=>`<div class="signal"><b>${x[0]}</b><span>${x[1]} · <span class="badge ${x[2]==='ACKNOWLEDGED'||x[2]==='DUPLICATE_SUPPRESSED'?'healthy':'watch'}">${x[2]}</span></span></div>`).join('')}
$('#goOnline').onclick=()=>{state.synced=true;$('#deviceNet').textContent='ONLINE';$('#queueCount').textContent='0 events';renderSync();addFeed('good','SUTRA EVENT SYNCED','Signed field receipt acknowledged; queue cleared','EDGE');toast('Signed offline event synchronized idempotently')};
$('#retrySync').onclick=()=>{if(!state.synced){toast('Reconnect before retrying')}else{renderSync();toast('Duplicate business event suppressed by idempotency key')}};
$('#deviceNext').onclick=()=>{state.sutraStep=(state.sutraStep+1)%4;let steps=[['Voice request captured','“मेरी सरसों की फसल के लिए मिट्टी रिपोर्ट के हिसाब से क्या करना है?”','ASR intent locally staged · farmer confirmation required'],['Verified context retrieved','Farmer / plot / SHC references resolved in demo adapter','No sensitive production data stored in this sandbox'],['Authorized rule path prepared','Agronomic values can only originate from approved deterministic services','RAJ-NUTRI may explain; it may not invent dosage'],['Signed receipt queued offline','Human-confirmed transaction sealed locally','Reconnect later; same business event is accepted once']];let s=steps[state.sutraStep];$('#deviceTitle').textContent=s[0];$('#deviceText').textContent=s[1];$('#deviceSub').textContent=s[2]};renderSync();
function renderIntegrations(){let arr=[['iFMS / DBT','Movement, sales and stock rail','CONTRACT_READY'],['FFS / DRS','Farmer application / dispute transaction rail','CONTRACT_READY'],['Soil Health Card','Soil parameters + State recommendation logic','CONTRACT_READY'],['AgriStack','Farmer / crop / plot references','CONTRACT_READY'],['IMD / weather','Weather features and alerts','CONTRACT_READY'],['BHASHINI','ASR / TTS / translation for assisted flows','CONTRACT_READY'],['Public Kharif 2026 fertilizer data','Embedded statewide aggregate evidence','LIVE_PUBLIC'],['Operational district values','Synthetic control-room demo data','DEMO_SANDBOX'],['TraceGraph event schema','Local evaluator implementation','DEMO_SANDBOX']];$('#integrations').innerHTML=arr.map(x=>`<div class="integration"><b>${x[0]}</b><p>${x[1]}</p><span class="badge ${x[2]==='LIVE_PUBLIC'?'healthy':x[2]==='DEMO_SANDBOX'?'watch':'violet'}">${x[2]}</span></div>`).join('')}
$('#simulateGps').onclick=()=>{WAREHOUSES[4].gps='Route deviation';WAREHOUSES[4].status='Attention';renderWarehouses();addFeed('bad','GPS GEOFENCE DEVIATION','Udaipur consignment exited expected corridor','LOGISTICS');renderLogistics();toast('Route anomaly added to logistics review queue')};
function modal(title,html){$('#modalTitle').textContent=title;$('#modalBody').innerHTML=html;$('#modal').classList.add('show')}$('#closeModal').onclick=()=>$('#modal').classList.remove('show');$('#modal').onclick=e=>{if(e.target===$('#modal'))$('#modal').classList.remove('show')};
function demoSequence(){if(state.demo)return;state.demo=true;$('#demoDock').classList.add('show');nav('command');let stages=[
 ['Stress signal detected','Rainfall recovery + mustard sowing acceleration injected for Sikar','14%',()=>{let d=DISTRICTS.find(x=>x.id==='sikar');d.p90=.081;d.cover=3.4;d.risk='critical';selectDistrict('sikar');addFeed('bad','KHARIF STRESS SIGNAL','Sikar DAP P90 cover fell to 3.4 days','DEMAND');updateKPIs()}],
 ['DemandCast recalculated','P90 demand widened; uncertainty visible to officer','30%',()=>{nav('demand');$('#forecastDistrict').value='sikar';$('#forecastFert').value='DAP';$('#rainRange').value=28;$('#areaRange').value=12;$('#salesRange').value=18;renderForecast();addFeed('warn','FORECAST ENVELOPE WIDENED','Sikar DAP P90 risk escalated','MODEL')}],
 ['Allocation candidate generated','Fairness-constrained transfer recommendation created','48%',()=>{nav('command');selectDistrict('sikar');toast('Officer recommendation ready — human approval required')}],
 ['Officer authorization','Transfer recommendation converted to auditable decision','64%',()=>{let d=selected(),t=transferSuggestion(d);approveTransfer(d,t)}],
 ['TraceGraph custody opened','Authorized movement receives tamper-evident custody events','78%',()=>{nav('trace');addFeed('good','TRACE CUSTODY OPENED','Transfer order linked to new custody event chain','TRACE')}],
 ['SUTRA field receipt captured offline','Dealer / field acknowledgement signed locally','91%',()=>{nav('sutra');state.synced=false;$('#deviceNet').textContent='OFFLINE';$('#queueCount').textContent='1 event';renderSync();addFeed('info','SUTRA OFFLINE RECEIPT','Signed field event queued locally','EDGE')}],
 ['Closed-loop sync complete','Same signed event retried; duplicate suppressed','100%',()=>{state.synced=true;$('#deviceNet').textContent='ONLINE';$('#queueCount').textContent='0 events';renderSync();$('#kpiIntegrity').textContent='99.1%';addFeed('good','CLOSED LOOP COMPLETE','Field acknowledgement synchronized idempotently','MISSION');setTimeout(()=>{nav('command');modal('Judge Demo Complete',`<div class="receipt">DEMO MISSION RECEIPT\n\nStress signal → P90 forecast → fair allocation → human approval → TraceGraph custody → SUTRA offline receipt → idempotent sync\n\nAll operational values are DEMO_SANDBOX. Public statewide fertilizer aggregates remain LIVE_PUBLIC.\n\nReceipt hash: ${simpleHash('URVARA-JUDGE-DEMO-'+Date.now())}</div>`);},600)}]
 ];let i=0;function step(){if(!state.demo||i>=stages.length){state.demo=false;$('#demoDock').classList.remove('show');return}let s=stages[i];$('#demoStage').textContent=s[0];$('#demoCopy').textContent=s[1];$('#demoProgress').style.width=s[2];s[3]();i++;state.demoTimer=setTimeout(step,1300)}step()}
$('#judgeDemo').onclick=demoSequence;$('#stopDemo').onclick=()=>{state.demo=false;clearTimeout(state.demoTimer);$('#demoDock').classList.remove('show');toast('Judge demo stopped')};
function reset(){location.reload()}$('#resetBtn').onclick=reset;
$$('[data-mapmode]').forEach(c=>c.onclick=()=>{$$('[data-mapmode]').forEach(x=>x.classList.remove('active'));c.classList.add('active');state.mapMode=c.dataset.mapmode;renderMap()});
function tickClock(){let d=new Date();$('#clock').textContent='Last refresh '+d.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit',second:'2-digit'})}setInterval(tickClock,1000);tickClock();
function boot(){let stages=[['Loading public fertilizer position…',18],['Building district operational twin…',38],['Verifying model / trace policies…',61],['Initializing offline field queue…',82],['Operations console ready',100]];let i=0;function next(){if(i>=stages.length){setTimeout(()=>{$('#boot').style.opacity=0;setTimeout(()=>$('#boot').remove(),300)},250);return}$('#bootmsg').textContent=stages[i][0];$('#bootpct').textContent=stages[i][1]+'%';$('#bootbar').style.width=stages[i][1]+'%';i++;setTimeout(next,240)}next()}
renderMap();renderRows();renderWorkbench();renderFeed();renderWarehouses();renderTransfers();renderLogistics();populateForecast();renderTimeline();renderIntegrations();updateKPIs();lookupFarmer();boot();